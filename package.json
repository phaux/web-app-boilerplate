{
  "name": "app",
  "version": "0.0.0",
  "license": "ISC",
  "scripts": {
    "build": "babel src -d app -x .ts,.tsx -D",
    "start": "run-p start:*",
    "start:server": "es-serve -w app",
    "start:babel": "babel src -d app -x .ts,.tsx -D -w",
    "test": "tsc -p . && tslint -p . && stylelint \"src/**/*.css\""
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/preset-react": "^7.0.0",
    "@babel/preset-typescript": "^7.1.0",
    "es-serve": "^0.9.0",
    "npm-run-all": "^4.1.3",
    "stylelint": "^9.4.0",
    "tslint": "^5.11.0",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "preact": "^8.3.1",
    "preact-compat": "^3.18.4"
  }
}
